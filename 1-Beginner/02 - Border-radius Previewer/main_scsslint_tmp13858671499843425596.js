const resizeBorderRadius = (border, ) => {
	console.log(border)
}
const controls = document.querySelectorAll('controls-wrapper').forEach(controlsSet => controlsSet.addEventListener('click', resizeBorderRadius));
